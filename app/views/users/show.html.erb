<h1 class="text-center mb-5">Welcome to <%= @user.username %>'s page</h1>

<div class="row justify-content-center">
  <div class="col-md-4 text-center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4 class="text-center mb-4"><%= @user.username %>'s articles</h4>
<%= will_paginate @user_articles, class: 'd-flex justify-content-center' %>
<%= render 'articles/article', obj: @user_articles %>
<%= will_paginate @user_articles, class: 'd-flex justify-content-center' %>
