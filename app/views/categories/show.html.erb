<h1 class="text-center mb-5"><%= 'Category ' + @category.name %></h1>

<%= will_paginate @category_articles, class: 'd-flex justify-content-center' %>
<%= render 'articles/article', obj: @category_articles %>
<%= will_paginate @category_articles, class: 'd-flex justify-content-center' %>
